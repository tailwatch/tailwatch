composer.json
composer.lock
phpcs_report.txt
phpcs.xml

.claude/

.cursor/

tools/

# React build artifacts (regenerated by `npm run build`)
admin-app/node_modules/
admin-app/build/
node_modules/

.instructions/

# Local integration smoke-test harness (contains live-site secrets — never commit)
_smoke-test/

docs/

# Local dev-server config (npm start)
.env
# Clean release staging folder (built by scripts/build-plugin.js)
/dist/
